# עדכון אפיון — המטרה האמיתית (10.9.2026)

אחרי כמה סבבים, המשתמש חידד את מטרת העל במילים שלו, וזה מה שמנחה את הפיתוח
מעכשיו - לא האפיון המקורי למטה כפשוטו:

> "מטרת העל של המערכת היא לזהות פעולות שחוזרות על עצמן — העברת קבצים, תיעוד
> לקוחות במערכת CRM, או פעולות יום-יומיות אחרות ידניות — שהיא תזהה אותן,
> תנתח אותן, ותמליץ איך אפשר לחסוך את השעות האלו (כלי / AI / אוטומציה).
> המטרה שזה ייצא לחברות, איפה שהעובד שלהם יכול לחסוך זמן, ובדשבורד רואים
> את כל הנתונים ומנתחים כל חודש כמה עובד מבזבז זמן ואיפה, ואיפה אפשר להכניס
> אוטומציה וטכנולוגיה שתפתור את הפעולה הידנית."

בפועל זה אומר: **המוצר הוא לא "תדרוך בוקר"** (זה רק log יומי גולמי) **והוא
לא Autopilot** (זה נשאר גבול בטיחות קבוע - שום פעולה אוטונומית בלי אישור).
המוצר הוא **מנוע זיהוי דפוסים + המלצות** - ראה `agent/src/patterns.js` +
טאב "המלצות אוטומציה" ב-GUI. מכוון לבעל עסק/מנהל שבודק חודשית איפה העובדים
מבזבזים זמן על עבודה ידנית חוזרת, לא (רק) לעובד עצמו.

**עדכון 0.5.0 (16.9.2026) - מחקר מתחרים:** השוואה ל-RescueTime/Timely/
Screenpipe/Viva Insights (ר' CHANGELOG.md למקורות) העלתה שני חוסרים
שרלוונטיים לכלי report-only (ולא לתחום ה-execution האוטונומי, שנשאר מחוץ
לתחום בכוונה): (1) ציון ביטחון (confidence) לכל דפוס - כמה ה"ודאות" שזה
דפוס אמיתי ולא רעש, לא ניקוד AI/ML, שילוב פשוט של כיסוי-ימים+צפיפות
(`computeConfidence`); (2) פילוח לפי קטגוריה - "לאן הזמן הולך בסך הכול"
ברמת קטגוריה (לא רק דפוס בודד), בדיוק העדכון החודשי שהאפיון למעלה מבקש
במפורש. שניהם תצוגה/דוח בלבד - שום פעולה חדשה לא נוספה.

---

# אפיון מקורי: AutoProcess Twin (סייען אוטומציה וכפיל דיגיטלי אוטונומי)

> נשמר כפי שהתקבל מהמשתמש ב-2026-09-07, לצורך רפרנס היסטורי בלבד - הפרק
> למעלה הוא המטרה המעודכנת. ראה `README.md` לסטטוס המימוש בפועל.

## 1. תקציר מנהלים וקונספט

מערכת AutoProcess Twin משלבת בין יכולת טכנית לניטור והבנת פעולות מערכת (Screen AI
Indexing, הקשות מקלדת) לבין בניית "צל דיגיטלי" (Autonomous Digital Twin) שלומד את
סגנון החשיבה, הטון וקבלת ההחלטות של המשתמש.

המטרה: לחסל את צוואר הבקבוק האנושי. המערכת לא רק בונה תסריטי אוטומציה, אלא מסוגלת
לעבוד בצורה עצמאית לחלוטין במצב "אוטו-פיילוט" (למשל בלילה), לענות למיילים, לתמחר
פרויקטים, לנהל משא ומתן ולסגור עסקאות, תוך חיסכון דרמטי בזמן והגדלת מכירות.

## 2. ארכיטקטורת המערכת המשולבת

```
[OS Drivers & Screen Capture Engine + Audio / Voice Capture]
       │
       ▼
[Privacy Filter & Masking Layer] (סינון מידע רגיש והחרגת יישומים)
       │
       ├─► [Keystroke, Mouse & UI Event Stream]
       ├─► [OCR & Vision Context Engine] (הבנת אלמנטים על המסך)
       └─► [NLP Context & Tone Analyzer] (ניתוח סגנון כתיבה, טון דיבור והחלטות עסקיות)
               │
               ▼
   [Cognitive Logic & Pattern Engine] (חיבור בין "איך לוחצים" ל-"למה מחליטים")
               │
               ▼
   [Autonomous Twin Agent] (מחולל אוטומציות + מודל קבלת החלטות עצמאי)
               │
               ├─► [Live Assist Mode] (הצעת אוטומציות וקיצורי דרך בזמן עבודה)
               └─► [Autopilot / Night Mode] (ריצה עצמאית ברקע, כתיבת מענה ופעולות ב-CRM)
                       │
                       ▼
           [Morning Briefing Dashboard] (דוח סיכום פעולות אקטיביות ואישורים חריגים)
```

## 3. ישויות ושדות נתונים מרכזיים

### א. פרופיל קוגניטיבי

| שדה | סוג | תיאור |
|---|---|---|
| profile_id | UUID | מזהה משתמש קצה |
| tone_of_voice | String/Vector | וקטור המייצג את סגנון ההתנסחות |
| pricing_boundaries | JSON | גבולות גזרה להנחות ותמחור |
| decision_rules | Array | חוקים שנלמדו מפעולות עבר |

### ב. תהליך מזוהה

| שדה | סוג | תיאור |
|---|---|---|
| process_id | UUID | מזהה ייחודי לתהליך |
| process_name | String | שם התהליך |
| detected_apps | Array | מערכות מעורבות |
| execution_mode | Enum | manual_trigger, live_assist, full_autopilot |

### ג. סשן אוטו-פיילוט

| שדה | סוג | תיאור |
|---|---|---|
| session_id | UUID | מזהה ריצת לילה/רקע |
| actions_taken | Integer | כמות פעולות עצמאיות שבוצעו |
| escalation_flags | JSON | פניות שהצריכו מעורבות אנושית |
| estimated_time_saved | Integer | דקות עבודה אנושית שנחסכו בסשן |

## 4. מנגנון הפעולה המשולב

**למידה רב-שכבתית:** שכבה טכנית (איפה מקליקים, איך שולפים נתונים) + שכבה קוגניטיבית
(סריקת תכתובות עבר, מודל שפה מקומי שמבין אינטרס עסקי).

**ריצה עצמאית (The Night Shift):** ה-Agent פותח מערכות, מנתח בקשות, בודק מחירון,
מחשב רווחיות, מנסח תשובה, שולח הודעה, מעדכן CRM — או יוצר טיוטה לאישור כשההחלטה
חורגת מהסמכות שהוגדרה.

## 5. תהליך עבודה וממשק משתמש

**Morning Briefing** — סיכום פעולות שבוצעו בלילה, עסקאות שנסגרו, הצעות ששלחו, ופניות
שממתינות לאישור.

**Guardrail Dashboard** — מסך שבו המשתמש מגדיר מה מותר ומה אסור (סכומי עסקה, אחוזי
הנחה, לקוחות VIP שלא לגעת בהם אוטומטית).

## 6. אבטחה, בקרות ומודל עסקי

- **Local & Isolated Execution** — הכל רץ On-Device, לא בענן ציבורי.
- **Air-Brakes** — בלם חירום שעוצר אוטומציה מיידית בזיהוי חריגה.
